<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dungeonmaster">

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DungeonTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".LoadingScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".instruments.counters.typical.Munchkin">
           </activity>
        <activity android:name=".instruments.counters.typical.MagicTheGathering" />
        <activity android:name=".instruments.counters.TypicalCounters" />
        <activity android:name=".instruments.counters.GameCounters" />
        <activity android:name=".tablegames.available.AvailableGames" />
        <activity android:name=".games.dnd.CreateCharacter" />
        <activity android:name=".games.dnd.DnDHelper" />
        <activity android:name=".instruments.Tools" />
        <activity android:name=".tablegames.TableGames" />
        <activity android:name=".menu.MainMenu"/>
        <activity android:name=".menu.Registration"/>
        <activity android:name=".menu.SignIn"/>
        <activity android:name=".instruments.dice.CubeScene">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>